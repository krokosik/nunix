{
  "nix.enableLanguageServer": true,
  "nix.serverPath": ["nix", "run", "nixpkgs#nixd"], // or "nixd", or ["executable", "argument1", ...]
  // LSP config can be passed via the ``nix.serverSettings.{lsp}`` as shown below.
  "nix.serverSettings": {
    "nixd": {
      "formatting": {
        "command": [
          "nix", "run", "nixpkgs#nixfmt"
        ],
      }
    }
  }
}